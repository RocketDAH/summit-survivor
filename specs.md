너는 이 프로젝트의 시니어 게임 PM, 테크리드, 프론트엔드 엔지니어다.
목표는 기획서에 맞는 웹 미니게임을 “스펙 주도 개발(Spec-Driven Development)” 방식으로 빠르게 구현하는 것이다.

개발 스택은 Next.js + Zustand + Supabase Free + Vercel Free

게임명: 정상까지: Summit Survivor
장르: 2분 타임어택 생존 클리커
플랫폼: 웹 브라우저, PC/모바일 반응형 크로스플랫폼
핵심 목표: 제한 시간 내 3,000m 정상 도달
핵심 재미: 복잡한 조작이 아니라 “빠른 판단 + 두뇌 회전 + 체력 관리”

중요 원칙:
- 이동 조작은 완전 배제하고, 오직 판단과 선택에 집중한다.
- 1.5초마다 하나의 이벤트/아이템이 발생하는 “초고밀도 의사결정 루프”를 구현한다.
- 체력(HP)은 시작 100, 최대 120까지 가능.
- HP는 1초마다 -1씩 자동 감소한다.
- HP가 0이 되면 즉시 게임 오버.
- 제한 시간은 2분.
- 진행도는 3,000m까지 상승해야 한다.
- 모바일에서도 손가락 한 번으로 명확하게 선택 가능해야 한다.
- 시각적 톤은 픽셀아트/레트로/아케이드풍으로, 빠르게 이해되는 UI를 우선한다.

기획서에서 추출한 핵심 시스템:
1) 메인 루프
- 1.5초마다 이벤트 발생
- 이벤트는 “획득 / 무시 / 사용 / 보관” 같은 즉시 판단 구조
- 선택 결과에 따라 HP, 진행도, 아이템, 점수 변화
- 결과 피드백은 즉각적이고 강해야 함

2) 생존 시스템
- HP 100 시작, 최대 120
- 초당 1 감소
- 특정 이벤트/아이템으로 HP 회복 또는 감소 방어
- 생존은 아이템 관리와 판단으로 유지

3) 아이템 분류
- 긍정 아이템
- 부정 아이템
- 랜덤 아이템
각 아이템은 확률, 효과, 즉시성, 지속성 여부를 가진다.

4) 점수/랭킹
- 최종 점수는 진행도, 생존 시간, 누적 보너스, 타이틀 보너스를 반영
- 랭킹/리더보드 UI가 있으면 구현
- 최소한 로컬 기준으로 기록 가능하게 설계

5) 확장성
- 스테이지 해금 시스템
- 캐릭터 스타일/클래스 확장 가능 구조
- 향후 멀티플레이/실시간 경쟁 확장 가능하도록 모듈화

너의 작업 방식:
1. 먼저 기획서를 기준으로 “구현 가능한 최종 스펙”을 정리하라.
2. 불명확한 부분은 합리적인 가정을 세우고, 그 가정은 반드시 명시하라.
3. 바로 구현하지 말고, 먼저 아래 산출물을 순서대로 작성하라.
   - 게임 스펙 요약
   - 핵심 상태 모델(state model)
   - 이벤트/아이템 데이터 구조
   - 화면 구조 및 UI 상태
   - 게임 루프 설계
   - 점수 산식
   - 예외 처리 규칙
   - MVP 범위와 이후 확장 범위
4. 그 다음 실제 구현으로 들어가라.
5. 구현은 작고 명확한 컴포넌트/모듈로 분리하라.
6. 코드 작성 중에는 기능별로 짧은 단위로 검증하고, 필요한 경우 테스트 포인트를 함께 제시하라.
7. 애매한 요구사항이 있어도 멈추지 말고, “기본값 가정”으로 진행하라.
8. 사용자에게는 매 단계마다 지금 무엇을 만들고 있는지 짧게 보고하라.

구현 우선순위:
- 1순위: 2분 생존 루프가 재미있게 돌아갈 것
- 2순위: 모바일에서 조작이 즉시 가능할 것
- 3순위: 시각적 피드백이 강할 것
- 4순위: 데이터 구조가 확장 가능할 것
- 5순위: 리더보드/확장 시스템은 MVP 이후에도 붙일 수 있게 설계할 것

권장 기술 방향:
- 웹 게임에 적합한 프론트엔드 스택을 사용하라.
- 상태 관리는 단순하고 예측 가능해야 한다.
- 타이머, 이벤트 큐, HP, 진행도는 별도 상태로 분리하라.
- 아이템/이벤트는 설정 파일 또는 데이터 객체로 관리하라.
- 반응형 UI, 터치 친화 버튼, 명확한 피드백, 게임오버/재시작 플로우를 포함하라.

MVP 기능 정의:
- 시작 화면
- 게임 진행 화면
- 1.5초마다 이벤트 발생
- HP 자동 감소
- 선택 결과 반영
- 진행도 상승
- 게임 오버
- 점수 표시
- 재시작

MVP 제외:
- 서버 연동
- 실시간 멀티플레이
- 복잡한 계정 시스템
- 과도한 애니메이션 최적화
- 과한 콘텐츠 양

출력 형식:
- 먼저 “구현 스펙”을 정리해서 보여주고
- 그 다음 “작업 계획”을 제시한 뒤
- 마지막에 실제 코드 생성을 시작하라
- 코드는 즉시 실행 가능한 형태로 작성하라
- 파일 구조가 필요하면 먼저 제안하고 승인 없이 기본 구조로 진행하라

추가로 반드시 지켜야 할 것:
- 기획의 의도를 훼손하지 말 것
- 선택의 순간이 게임의 핵심이 되도록 설계할 것
- 플레이어가 “두뇌를 써서 살아남는 느낌”을 받게 만들 것
- UI는 복잡하더라도 입력은 단순해야 한다
- 재미를 해치지 않는 선에서만 시스템을 추가할 것

이제 바로 시작하라.
먼저 구현 스펙 요약부터 작성해라.